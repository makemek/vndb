{
  "name": "vndb",
  "version": "0.1.0",
  "description": "A wrapper for VNDB API",
  "main": "index.js",
  "dependencies": {},
  "devDependencies": {
    "chai": "^3.5.0",
    "chai-things": "^0.2.0",
    "co-mocha": "^1.1.3",
    "eslint": "^3.7.1",
    "eslint-config-airbnb-base": "^8.0.0",
    "eslint-plugin-import": "^1.16.0",
    "http-server": "^0.9.0",
    "istanbul": "^0.4.5",
    "mocha": "^3.1.0",
    "nodemon": "^1.10.2",
    "sinon": "^1.17.6",
    "sinon-chai": "^2.8.0"
  },
  "scripts": {
    "lint": "eslint .",
    "lint:watch": "nodemon --exec 'npm run --silent lint || true'",
    "test": "mocha test/unit/**/*.js",
    "test:watch": "nodemon --exec 'npm run --silent test || true'",
    "test-integration": "mocha test/integration/**/*.js",
    "test-integration:watch": "nodemon --exec 'npm run --silent test-integration || true'"
  },
  "author": "Permagate",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Permagate/vndb"
  },
  "bugs": "https://github.com/Permagate/vndb/issues"
}
